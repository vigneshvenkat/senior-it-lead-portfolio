/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
    basePath: '/vigneshvenkat.github.io'
};

export default nextConfig;
